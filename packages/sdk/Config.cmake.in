# Config.cmake.in Template

@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# Add dependencies (if needed)
# find_dependency(SomeDependency REQUIRED)

# Import targets
include("${CMAKE_CURRENT_LIST_DIR}/${PROJECT_NAME}Targets.cmake")

# Provide information to the package user
set(${PROJECT_NAME}_INCLUDE_DIRS "@CMAKE_INSTALL_PREFIX@/include")
set(${PROJECT_NAME}_LIBRARY_DIRS "@CMAKE_INSTALL_PREFIX@/lib")
set(${PROJECT_NAME}_LIBRARIES ${PROJECT_NAME}::${sdkTarget})
set(${PROJECT_NAME}_STATIC_LIBRARIES ${PROJECT_NAME}::${sdkTargetStatic})